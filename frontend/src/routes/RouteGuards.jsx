import{Navigate}from"react-router-dom";import{useAuth}from"../context/AuthContext";import{protectedRouteState,roleRouteState}from"../authRouting";const Loading=()=> <div className="loading">Loading secure workspace…</div>;export function ProtectedRoute({children}){const state=protectedRouteState(useAuth());return state.type==="loading"?<Loading/>:state.type==="allow"?children:<Navigate to={state.to} replace/>}export function RoleRoute({role,children}){const state=roleRouteState(useAuth(),role);return state.type==="loading"?<Loading/>:state.type==="allow"?children:<Navigate to={state.to} replace/>}
