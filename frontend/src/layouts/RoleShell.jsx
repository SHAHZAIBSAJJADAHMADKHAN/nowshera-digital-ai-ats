import{Link}from"react-router-dom";import{LogOut}from"lucide-react";import{useAuth}from"../context/AuthContext";export function RoleShell({role}){const{signOut,profile}=useAuth();return <main className="workspace"><aside><Link className="brand" to="/">Nowshera <b>Digital</b></Link><p>{role} workspace</p><Button onClick={signOut}><LogOut size={16}/> Sign out</Button></aside><section><p className="eyebrow">Welcome back</p><h1>{profile?.email||`${role} workspace`}</h1><article><h2>Your workspace is ready</h2><p>Core ATS modules will appear here as they are introduced. Access stays governed by your assigned role.</p></article></section></main>}const Button=({children,...p})=><button className="link-button" {...p}>{children}</button>;
