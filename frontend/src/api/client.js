import {config}from"../config";export class ApiError extends Error{constructor(message,status){super(message);this.status=status}}export async function api(path,{token,body,headers={},...options}={}){const form=body instanceof FormData;const response=await fetch(`${config.apiBaseUrl}${path}`,{...options,body:body&&(form?body:JSON.stringify(body)),headers:{...(body&&!form?{"Content-Type":"application/json"}:{}),...(token?{Authorization:`Bearer ${token}`}:{ }),...headers}});if(response.status===204)return null;const data=await response.json().catch(()=>null);if(!response.ok)throw new ApiError(data?.detail||"Something went wrong. Please try again.",response.status);return data}
